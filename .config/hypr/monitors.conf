# Use 1.2x scaling for any display not overridden by the docked layout watcher.
# The fallback also covers panels that Hyprland reports as "Unknown-1".
monitor = , preferred, auto, 1.2

# Uncomment and adjust for HiDPI / retina displays (e.g. Apple T2 internal):
# monitor = eDP-1, 2560x1600@60, 0x0, 2
# env = GDK_SCALE,2
