# Application bindings
bind = SUPER, f, fullscreen
bind = SUPER, return, exec, ~/.config/hypr/scripts/split-open.sh
bind = SUPER, B,       exec, uwsm app -- zen-browser
bind = SUPER SHIFT, B, exec, uwsm app -- zen-browser --private

# macOS-style clipboard (Super = Cmd)
bindd = SUPER, C, Copy,  sendshortcut, CTRL, Insert,
bindd = SUPER, V, Paste, sendshortcut, SHIFT, Insert,
bindd = SUPER CTRL, V, Clipboard manager, exec, omarchy-launch-walker -m clipboard

# ── Persistent split direction ───────────────────────────────────────────────
# Super+Z = horizontal (side by side). Super+X = vertical (stacked).
# Mode persists — keep pressing Super+Enter to keep splitting that way.
bind = SUPER, Z, exec, echo h > ~/.cache/hypr-split-mode
bind = SUPER, X, exec, echo v > ~/.cache/hypr-split-mode
# Toggle existing tile pair direction
bind = SUPER, T, layoutmsg, togglesplit

# ── Focus (Alt = Option, hjkl) ───────────────────────────────────────────────
bind = ALT, h, movefocus, l
bind = ALT, j, movefocus, d
bind = ALT, k, movefocus, u
bind = ALT, l, movefocus, r

# ── Move window (Super+Shift+hjkl) ──────────────────────────────────────────
bind = SUPER SHIFT, h, movewindow, l
bind = SUPER SHIFT, j, movewindow, d
bind = SUPER SHIFT, k, movewindow, u
bind = SUPER SHIFT, l, movewindow, r

# ── Resize submap ────────────────────────────────────────────────────────────
bind = SUPER, R, submap, resize
submap = resize
binde = , h, resizeactive, -20 0
binde = , l, resizeactive,  20 0
binde = , k, resizeactive,  0 -20
binde = , j, resizeactive,  0  20
bind  = , escape, submap, reset
submap = reset

# Note: Super+Shift+[1-9] to move window to workspace is provided by
# omarchy's tiling-v2.conf (movetoworkspace bindings already active).
